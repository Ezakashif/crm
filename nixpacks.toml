# Fallback / Nixpacks hints when Railpack is not used.
# Ensures Laravel's public/ directory is the web root and Node is available for Vite.

[variables]
NIXPACKS_PHP_ROOT_DIR = "/app/public"
NIXPACKS_NODE_VERSION = "22"

[phases.setup]
nixPkgs = ["python3", "python3Packages.pillow"]
